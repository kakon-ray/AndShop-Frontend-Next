import { Button, Card, Form } from "react-bootstrap";
import Link from "next/link"
import PasswordResetSubmitCard from "../../../component/AuthCard/PasswordResetSubmitCard";

const Page = () => {
    return (
        <div>
            <PasswordResetSubmitCard />
        </div>
    );
};

export default Page;